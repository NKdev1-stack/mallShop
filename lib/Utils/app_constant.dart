// Used to define values that are fixed and don't need to be reassigned or recalculated

import 'dart:ui';

class AppConstant {
  // Static Mean these variables are accessable any where in the location..without creating class instances
  static String appMainName ='MallShop';
  static String appPoweredBy = "Powered by NKDev";
  static const appMaincolor = Color.fromARGB(255, 255, 94, 0);
  static const appSecondarycolor = Color.fromARGB(255, 255, 145, 0);
  static const appTextcolor = Color.fromARGB(255, 255, 255, 255);
  static const statusBarColor = Color.fromARGB(255, 255, 255, 255);

}